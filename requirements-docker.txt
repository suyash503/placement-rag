# Direct dependencies only. Transitives resolve at build time, which avoids the
# platform mismatch you get from exporting a lockfile resolved on another OS.
# torch is installed separately from the CPU index before this file is used.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
sse-starlette>=2.1.3

pymongo>=4.10.0
certifi>=2024.8.30

langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-mongodb>=0.6.0
langchain-huggingface>=0.1.0
langchain-text-splitters>=0.3.0
langchain-google-genai>=2.0.0

sentence-transformers>=3.2.0

pandas>=2.2.0
python-dotenv>=1.0.1
